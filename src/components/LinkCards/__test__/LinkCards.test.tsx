import { cleanup } from "@testing-library/react";

describe("LinkCards", () => {
  afterEach(() => {
    jest.clearAllMocks();
    cleanup();
  });

  test.todo("should navigate to \"create card\" page");
});
