import { cleanup } from "@testing-library/react";

describe("useCheckAuth", () => {
  afterEach(() => {
    jest.clearAllMocks();
    cleanup();
  });

  test.todo("should navigate to \"LinkCard\" page");

  test.todo("should navigate to \"Login\" page");

  test.todo("should navigate to \"Login\" page if check is failed");
});
